declare module 'bidi-js' {
  // Minimal type surface we need
  export function reorderVisually(input: string): string;
  const _default: { reorderVisually: (input: string) => string };
  export default _default;
}


